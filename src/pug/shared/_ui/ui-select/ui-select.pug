mixin ui-select(mods, ...data)
  -
    const baseName = 'ui-select'
    let allMods = ''
    if (typeof mods !== 'undefined' && mods) {
      const modsList = mods.split(',')
      for (const element of modsList) {
        allMods += element.trim()[0] === '_' ? ' ' + baseName + element.trim() : ' ' + element.trim() + '__' + baseName
      }
      allMods = allMods.trim()
    }

  .ui-select(class=allMods)
    select&attributes(attributes)
      option
      p Lorem ipsum dolor sit amet consectetur adipisicing elit. Assumenda enim suscipit velit minima veritatis error accusamus aperiam illo dolores quibusdam iste, voluptatum possimus nulla, temporibus, est deleniti eos nemo minus.
